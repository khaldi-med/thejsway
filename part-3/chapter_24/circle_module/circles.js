'use strict';

module.exports.circumference = (radius) =>
  `The circumference of a circl of ${radius} is ${2 * 3.14 * radius}`;
module.exports.area = (radius) => `Its area is ${3.14 * radius * radius}`;
